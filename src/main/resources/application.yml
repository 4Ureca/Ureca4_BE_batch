spring:
  batch:
    jdbc:
      initialize-schema: always
    job:
      enabled: false

  kafka:
    consumer:
      group-id: crm-group
      auto-offset-reset: earliest
    producer:
      acks: all

server:
  port: 8090